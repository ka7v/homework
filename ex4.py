from random import randint
def matrix(n:int):
    mat = []
    for i in range(n):
        row = [randint(100, 999) for i in range(5)]
        mat.append(row)
    return mat


def re_matrix(ls1:list, ls2:list):
    my_matrix = []
    for i in range(len(ls())):
        for i in range(0, len(m))
        for j in range(len(matrix(ls)):
            if ls[i] > ls[j]:

